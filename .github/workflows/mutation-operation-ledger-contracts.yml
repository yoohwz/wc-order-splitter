name: Mutation operation ledger contracts

on:
  push:
    branches:
      - main
      - 'codex/**'
  pull_request:

permissions:
  contents: read

jobs:
  operation-ledger-contracts:
    name: Operation ledger (PHP ${{ matrix.php }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php: ['7.4', '8.1', '8.3']

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none

      - name: Lint operation ledger files
        shell: bash
        run: |
          set -euo pipefail
          php -l inc/mutation-v2/class-wcos-v2-lease-lock.php
          php -l inc/mutation-v2/class-wcos-v2-operation-record.php
          php -l inc/mutation-v2/class-wcos-v2-operation-ledger.php
          php -l tests/v2/operation-ledger-contract-test.php

      - name: Run operation ledger contracts
        run: php tests/v2/operation-ledger-contract-test.php
