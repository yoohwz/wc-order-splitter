name: Mutation snapshot comparator contracts

on:
  push:
    branches:
      - main
      - 'codex/**'
  pull_request:

permissions:
  contents: read

jobs:
  snapshot-comparator-contracts:
    name: Snapshot comparator (PHP ${{ matrix.php }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        php: ['7.4', '8.1', '8.3']

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: none

      - name: Lint snapshot comparator files
        shell: bash
        run: |
          set -euo pipefail
          php -l inc/mutation-v2/class-wcos-v2-snapshot-comparator.php
          php -l tests/v2/snapshot-comparator-contract-test.php

      - name: Run snapshot comparator contracts
        run: php tests/v2/snapshot-comparator-contract-test.php
